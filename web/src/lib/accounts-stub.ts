// Empty stub. Aliased in next.config.ts as `accounts` to satisfy Turbopack's
// static analysis of the wagmi `tempo` connector's dynamic import. The
// connector is unused at runtime in this app.
export {}
