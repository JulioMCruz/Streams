// Stub for the optional `accounts` dynamic import in @wagmi/core's tempo
// connector. The connector throws a runtime error if instantiated; this stub
// only exists so bundlers can resolve the module name during static analysis.
module.exports = {}
