services:
  bot:
    build:
      context: .
      dockerfile: deploy/Dockerfile.bot
    image: streams-bot:slim
    container_name: streams-bot
    restart: unless-stopped
    env_file:
      - .env
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
